import React from 'react';
import StoresList from './storeList';

const EggMatrix = () => {
  
    return (
        <div>
            <StoresList />
        </div>
    );
};

export default EggMatrix;
